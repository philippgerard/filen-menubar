{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the app's local webview windows",
  "windows": ["login", "activity"],
  "permissions": [
    "core:default"
  ]
}
