{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the transient login window",
  "windows": ["login"],
  "permissions": [
    "core:default"
  ]
}
